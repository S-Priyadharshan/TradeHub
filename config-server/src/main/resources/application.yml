spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/S-Priyadharshan/config-repo
          default-label: main
          clone-on-start: true

server:
  port: 8888

